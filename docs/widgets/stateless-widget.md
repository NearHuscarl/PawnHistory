# StatelessWidget

Composition-only widget base.

| Member | Default | Does |
| --- | --- | --- |
| `StatelessWidget(string key = null)` | `key = null` | Creates a stateless composition node. |
| `Build(ctx)` | abstract | Returns the child tree for this pass. |
